<div class="filter-sidebar custom-sidebar">
  <div class="sidebar-content">

  <!-- Title -->
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h4 class="mb-0">Filter</h4>
    <button class="btn btn-link text-primary p-0">Reset</button>
  </div>

  <!-- Categories -->
  <div class="mb-4">
    <h5 class="mb-3">Categories</h5>
    <div class="list-group">
      <div
        *ngFor="let category of categories"
        class="list-group-item border-0 px-0"
      >
        <div class="form-check">
          <input
            class="form-check-input"
            type="checkbox"
            [id]="'cat' + category.id"
          />
          <label class="form-check-label" [for]="'cat' + category.id">
            {{ category.name }} ({{ category.count }})
          </label>
        </div>
      </div>
    </div>
  </div>

  <!-- Fulfillment -->
  <div class="mb-4">
    <h5 class="mb-3">Fulfillment</h5>
    <div class="dropdown">
      <button
        class="btn btn-outline-secondary dropdown-toggle w-100"
        type="button"
        data-bs-toggle="dropdown"
      >
        choose size
      </button>
      <ul class="dropdown-menu w-100">
        <li *ngFor="let size of sizes">
          <div class="form-check px-3">
            <input
              class="form-check-input"
              type="checkbox"
              [id]="'size' + size"
            />
            <label class="form-check-label" [for]="'size' + size">
              {{ size }}
            </label>
          </div>
        </li>
      </ul>
    </div>
  </div>

  <!-- Size -->
  <div class="mb-4">
    <h5 class="mb-3">Size</h5>
    <div class="dropdown">
      <button
        class="btn btn-outline-secondary dropdown-toggle w-100"
        type="button"
        data-bs-toggle="dropdown"
      >
        choose size
      </button>
      <ul class="dropdown-menu w-100">
        <li *ngFor="let size of sizes">
          <div class="form-check px-3">
            <input
              class="form-check-input"
              type="checkbox"
              [id]="'size' + size"
            />
            <label class="form-check-label" [for]="'size' + size">
              {{ size }}
            </label>
          </div>
        </li>
      </ul>
    </div>
  </div>

  <!-- Color -->
  <div class="mb-4">
    <h5 class="mb-3">Color</h5>
    <div class="dropdown">
      <button
        class="btn btn-outline-secondary dropdown-toggle w-100"
        type="button"
        data-bs-toggle="dropdown"
      >
        choose size
      </button>
      <ul class="dropdown-menu w-100">
        <li *ngFor="let size of sizes">
          <div class="form-check px-3">
            <input
              class="form-check-input"
              type="checkbox"
              [id]="'size' + size"
            />
            <label class="form-check-label" [for]="'size' + size">
              {{ size }}
            </label>
          </div>
        </li>
      </ul>
    </div>
  </div>

  <!-- Gender -->
  <div class="mb-4">
    <h5 class="mb-3">Gender</h5>
    <div class="dropdown">
      <button
        class="btn btn-outline-secondary dropdown-toggle w-100"
        type="button"
        data-bs-toggle="dropdown"
      >
        choose size
      </button>
      <ul class="dropdown-menu w-100">
        <li *ngFor="let size of sizes">
          <div class="form-check px-3">
            <input
              class="form-check-input"
              type="checkbox"
              [id]="'size' + size"
            />
            <label class="form-check-label" [for]="'size' + size">
              {{ size }}
            </label>
          </div>
        </li>
      </ul>
    </div>
  </div>

  <!-- Brand -->
  <div class="mb-4">
    <h5 class="mb-3">Brand</h5>
    <div class="dropdown">
      <button
        class="btn btn-outline-secondary dropdown-toggle w-100"
        type="button"
        data-bs-toggle="dropdown"
      >
        choose size
      </button>
      <ul class="dropdown-menu w-100">
        <li *ngFor="let size of sizes">
          <div class="form-check px-3">
            <input
              class="form-check-input"
              type="checkbox"
              [id]="'size' + size"
            />
            <label class="form-check-label" [for]="'size' + size">
              {{ size }}
            </label>
          </div>
        </li>
      </ul>
    </div>
  </div>

  <!-- Price Range -->
  <div class="mb-4">
    <h5 class="mb-3">Price Range</h5>
    <div class="px-3">
      <input type="range" class="form-range" min="0" max="1000" />
      <input type="range" class="form-range" min="0" max="1000" />
      <div class="d-flex justify-content-between small">
        <!-- يمكنك إضافة قيم هنا -->
      </div>
    </div>
  </div>

  <!-- Rating -->
  <div class="mb-4">
    <h5 class="mb-3">Rating</h5>
    <div class="list-group">
      <div
        *ngFor="let rating of [5, 4, 3, 2, 1]"
        class="list-group-item border-0 px-0"
      >
        <div class="form-check">
          <input
            class="form-check-input"
            type="radio"
            name="rating"
            [id]="'rate' + rating"
          />
          <label class="form-check-label" [for]="'rate' + rating">
            <i
              *ngFor="let star of [1, 2, 3, 4, 5]"
              class="bi bi-star{{ star <= rating ? '-fill' : '' }} text-warning"
            ></i>
          </label>
        </div>
      </div>
    </div>
  </div>
  </div>
</div>
